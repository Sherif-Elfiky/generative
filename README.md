extract-resume -> turns your pdf to txt
Run with **python3 extract-resume main.py**
all other files -> build a prompt with the text, and send it to Google's Gemini model via the AI SDK. 
Put your API key in a .env file as GOOGLE_GENERATIVE_AI_API_KEY.
Run with **npm start**
